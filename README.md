# Clinic Pro - ລະບົບຈັດການຄລີນິກ

ລະບົບຈັດການຄລີນິກທີ່ທັນສະໄໝ ພັດທະນາດ້ວຍ Nuxt 4, Prisma, MySQL, ແລະ TypeScript.

## ✨ ຄຸນສົມບັດຫຼັກ

### 📦 ການຈັດການສິນຄ້າ
- ເບິ່ງ Stock ສິນຄ້າ
- ແຈ້ງເຕືອນເມື່ອ Stock ໃກ້ໝົດ ຫຼື ໝົດ
- ຈັດການປະເພດສິນຄ້າ (ເມັດ, ແຜ່ນ, ອັນ, ຖົງ, ...)

### 👥 ການຈັດການລູກຄ້າ
- ເພີ່ມລູກຄ້າໃໝ່ (ຊື່, ນາມສະກຸນ, ເບີໂທ, ທີ່ຢູ່)
- ກວດສອບລູກຄ້າເກົ່າ (ແຈ້ງເຕືອນຖ້າເບີໂທມີແລ້ວ)
- ເບິ່ງປະຫວັດການໃຊ້ບໍລິການ

### 💰 ການຂາຍ
- ລະບົບ POS ທີ່ງ່າຍຕໍ່ການໃຊ້ງານ
- ເລືອກສິນຄ້າ ແລະ ບໍລິການ
- ສະໜັບສະໜູນ ໂປຣໂມຊັ່ນ/ສ່ວນຫຼຸດ
- ພິມໃບບິນ
- ສະຖານະການຈ່າຍ: ຈ່າຍ, ບໍ່ຈ່າຍ, ໂອນ

### 📊 ລາຍງານ (Admin ເທົ່ານັ້ນ)
- ລາຍງານຍອດຂາຍປະຈຳວັນ
- ລາຍງານຍອດຂາຍປະຈຳອາທິດ
- ລາຍງານຍອດຂາຍປະຈຳເດືອນ
- ລາຍງານຍອດຂາຍປະຈຳປີ

### 👤 ການຈັດການຜູ້ໃຊ້
- ແຍກສິດ Admin ແລະ Employee
- Admin: ເຫັນທັງໝົດ, ເບິ່ງລາຍງານ, ຈັດການຜູ້ໃຊ້
- Employee: ບໍ່ເຫັນລາຍງານ ແລະ ການຈັດການຜູ້ໃຊ້

### 🎁 ໂປຣໂມຊັ່ນ
- ເພີ່ມໂປຣໂມຊັ່ນ (ຊື່, ລາຍລະອຽດ, ລາຄາ/ເປີເຊັນ)
- ກຳນົດວັນເລີ່ມຕົ້ນ ແລະ ສິ້ນສຸດ

### 💅 ບໍລິການ
- ເພີ່ມບໍລິການຂອງຮ້ານ (ຊື່, ລາຍລະອຽດ, ລາຄາ)

## 🛠️ ເທັກໂນໂລຢີ

- **Frontend**: Nuxt 4, Vue 3, TypeScript
- **Styling**: Tailwind CSS
- **Database**: MySQL + Prisma ORM
- **Authentication**: JWT + bcrypt
- **Icons**: @nuxt/icon (Lucide Icons)

## 📁 ໂຄງສ້າງໂປຣເຈັກ

```
clinic/
├── app/
│   ├── assets/css/       # CSS ແລະ Tailwind
│   ├── components/       # Vue Components
│   ├── composables/      # Composables (useAuth, useCart, ...)
│   ├── layouts/          # Layouts (default, auth)
│   ├── middleware/       # Route middlewares
│   ├── pages/            # Pages (login, pos, products, ...)
│   └── types/            # TypeScript types
├── prisma/
│   ├── schema.prisma     # Database schema
│   └── seed.ts           # Seed data
├── server/
│   ├── api/              # API routes
│   └── utils/            # Server utilities (prisma, auth)
└── ...
```

## 🚀 ການຕິດຕັ້ງ

### 1. Clone ໂປຣເຈັກ

```bash
git clone <repository-url>
cd clinic
```

### 2. ຕິດຕັ້ງ Dependencies

```bash
npm install
```

### 3. ຕັ້ງຄ່າ Environment Variables

ສ້າງໄຟລ໌ `.env` ໃນ root folder:

```env
# Database Configuration
DATABASE_URL="mysql://username:password@localhost:3306/clinic_db"

# JWT Secret
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
```

### 4. ສ້າງ Database

```bash
# ສ້າງ database ໃນ MySQL ກ່ອນ
mysql -u root -p -e "CREATE DATABASE clinic_db;"

# Push schema ໄປ database
npm run db:push

# Generate Prisma Client
npm run db:generate

# Seed ຂໍ້ມູນເລີ່ມຕົ້ນ
npm run db:seed
```

### 5. ລັນ Development Server

```bash
npm run dev
```

ເປີດ browser ທີ່ http://localhost:3000

## 🔐 ຂໍ້ມູນເຂົ້າສູ່ລະບົບ (ຫຼັງ Seed)

| Role     | Username   | Password     |
|----------|------------|--------------|
| Admin    | admin      | admin123     |
| Employee | employee   | employee123  |

## 📜 Scripts

```bash
# Development
npm run dev           # ລັນ dev server

# Database
npm run db:generate   # Generate Prisma Client
npm run db:push       # Push schema to database
npm run db:migrate    # Run migrations
npm run db:seed       # Seed database
npm run db:studio     # Open Prisma Studio

# Production
npm run build         # Build for production
npm run preview       # Preview production build
```

## 📱 Responsive Design

ລະບົບຖືກອອກແບບໃຫ້ Responsive ສຳລັບທຸກ devices:
- Desktop: Drawer ເປີດຕະຫຼອດ
- Mobile/Tablet: Drawer ປິດໄດ້ ກົດປຸ່ມເມນູເພື່ອເປີດ

## 🎨 ການອອກແບບ

- Dark Theme ທີ່ສວຍງາມ
- Animations ແລະ Transitions
- Modern UI/UX
- ສະໜັບສະໜູນພາສາລາວ

## 📄 License

MIT License
